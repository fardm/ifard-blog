---
title: ساخت گزارش عملکرد شخصی در ابسیدین
aliases:
  - ساخت گزارش عملکرد شخصی در ابسیدین
created: 2025-01-05
publish: true
status: 🌱
tags:
comments: true
modified: 2026-06-09
draft: false
image: "[[create-report.webp]]"
---
![[create-report.webp]]

یکی از مفید ترین کارهایی که با ابسیدین انجام میدم ساخت **گزارش عملکرد شخصی** است. این گزارش به من کمک می‌کنه اشراف کاملی نسبت به فعالیت های خودم داشته باشم و با اختیار داشتن جزئیات بتونم عملکرد خودم رو دقیق تر ارزیابی کنم.

نمونه این گزارش‌ها رو می‌تونید در [[reports|📋گزارش‌های شخصی]] ببینید.

اگه می‌خواید از این روش استفاده کنید، توی این یادداشت کامل توضیح دادم تا چه طور از این مدل گزارش ها بسازید.

<br>

## گام اول: وارد کردن اطلاعات
در مرحله اول باید اطلاعات‌تون رو وارد کنید. گزارش‌های من معمولا دو بخش دارن. بخش اول **محتواهایی که مصرف کردم**. مثل کتاب، پادکست، دوره، فیلم و سریال. بخش دوم **ردیابی عادت‌های روزمره**. مثل ورزش، مطالعه و سوشال مدیا.

این دو مورد رو تو یادداشت‌های مستقل توضیح دادم:

```cardnote
{
  "title": "ساخت هبیت ترکر در ابسیدین",
  "image": "/assets/images/habit-tracker.webp",
  "link": "./habit-tracker-in-obsidian"
}
{
  "title": "وارد کردن محتوا در ابسیدین",
  "image": "/assets/images/add-content-obsidian.webp",
  "link": "./add-content-obsidian"
}
```

برای اینکه این آموزش راحت تر باشه من روی همون تمپلیت هبیت ترکر توضیح میدم که پلاگین ها از قبل نصب و تنظیم شدند. پس:
1. این [ریپازیتوری](https://github.com/fardm/Obsidian-Habit-Tracker) رو دانلود کنید و با ابسیدین باز کنید.
2. عادت‌ها و محتواهاتون رو وارد کنید.
3. برای تفکیک فایل‌ها از تگ استفاده کنید. مثلا فایل های من اینطوری میشه:
```
#Journal

Resources
├── #Books
├── #Podcast
├── #Movies
└── #Media
```
4. به تنظیمات پلاگین پرشین کلندر برید. تمپلیت سالیانه رو روی `yearly+report` بذارید.
5. روی عدد امسال در تقویم پرشین کلندر کلیک کنید تا فایل سالیانه ساخته بشه.

تمام! حالا یه فایل داریم که گزارش عملکرد ما رو در امسال نشون میده.

<br>

## گام دوم: شخصی سازی تمپلیت
این تمپلیت سه تا بخش داره که میتونید هر کدوم رو ویرایش کنید:

### بخش در یک نگاه
این بخش با پلاگین dataview ساخته شده. این دستور برای استخراج آمار کتاب هست:
````
```dataview
TABLE WITHOUT ID
	length(rows) + " جلد کتاب" as count,  // تعداد کل کتاب ها رو محاسبه میکنه
	sum(rows.pages) + " صفحه" as sum // تمام پراپرتی های pages رو جمع میزنه
FROM #Book  // مشخص میکنه اطلاعات از تگ book استخراج بشن
WHERE date >= date("2025-03-21") AND date <= date("2026-03-20") // بازه زمانی رو مشخص میکنه
GROUP BY "" // همه اطلاعات رو توی یک ردیف نمایش میده
```
````

- از بخش FROM تگی که استفاده کردید رو وارد کنید. اگر برای کتاب ها از تگ Book استفاده کردید همین دستور براتون کار میکنه. اگر از تگ دیگه ای استفاده کردید اون رو جایگزین کنید.
- از بخش WHERE تاریخ شروع و پایان رو باید وارد کنید تا فقط گزارش یکسال مشخص رو نمایش بده نه همه کتاب هایی که خوندید.
- دستور `sum(rows.pages)` هم همه پراپرتی های pages رو جمع میزنه. من توی این پراپرتی تعداد صفحات کتاب ها رو نوشتم. اگر از عنوان دیگه ای برای تعداد صفحات استفاده کردید اون رو جایگزین کنید.

ساختار باقی موارد هم مثل همینه. فقط برای فیلم مقدار مدت زمان رو جمع میزنه.

<br>

### بخش محتواها
این بخش با پلاگین base ساخته شده. تنظیماتش بسیار ساده است. توی همون رابط کاربری میتونید ادیتش کنید.
از منوی Filter می تونید نمایش فایل ها رو محود کنید. یک تگ خاص مثلا Book رو فیلتر میکنیم. تاریخ رو هم بین بازه یکسال مشخص تنظیم میکنیم. 

از بخش  Properties هم میتونید محتویات کارت رو مشخص کنید که چه مواردی رو نمایش بده.

<br>

### بخش عادت‌ها
این بخش با سه پلاگین دیتاویو، ترکر و کانتریببوشن گراف ساخته شده. شخصی سازی این مورد رو کامل داخل ویدئوی [[habit-tracker-in-obsidian|ساخت هبیت ترکر در ابسیدین]] توضیح دادم.

<br><br>


%% 

## انتشار در کوارتز
من برای انتشار گزارش ها در کوارتز از پلاگین [dataview serializer](https://github.com/dsebastien/obsidian-dataview-serializer) استفاده میکنم چون خود پلاگین دیتاویو توی کوارتز پشتیبانی نمیشه. (مگر با پلاگین کوارتز سینکر که من ازش استفاده نمی کنم).
توی نسخه 5 کوارتز بیس ابسیدین پشتیبانی میشه و میتونید دیتابیس محتوا ها رو هم راحت منتشر کنید اما چون من هنوز آپدیت نکردم از همون دیتاویو سریالیز برای نمایش کارتی محتواها استفاده می کنم.
از اون بخش هیت مپ کلندر ها هم اسکرین میگیرم و توی کوارتز قرار میدم.

البته میشد همه رو با اسکرین شات انجام داد ولی من ترجیح دادم داده ها داینامیک باشه چون ممکنه یه تغییراتی توش بدم.



برای سهولت در انجام این مرحله هم یک فایل آماده کردم که دستورات از قبل آماده است فقط کافیه اون ها رو شخصی سازی کنید.


پس:
1. فایل [Report-Template-Quartz-md](https://gist.github.com/fardm/6916510149112ae21076031ff1b78445#file-report-template-quartz-md) رو دانلود کرده و به والت ابسیدین خودتون اضافه کنید. یعنی پوشه content در کوارتز.
2. پلاگین dataview serializer رو نصب و فعال کنید.
3. فایل [card-view](https://github.com/fardm/ifard-blog/blob/v4/quartz/styles/_card-view.scss) رو دانلود و در مسیر `quartz/styles` قرار بدید.
4. بعد کد زیر رو به ابتدای فایل `custom.scs` اضافه کنید.
```scss
@use "./_card-view.scss";
```

(فایل card-view جدول ها رو تبدیل به نمای کارتی میکنه. [[quartz-customization#نمای کارتی|اینجا]] بیشتر توضیح دادم)


تمام! حالا میتونید گزارشتون رو شخصی سازی کنید.

<br> 

### چه طور شخصی سازی کنیم؟

ویرایش این فایل مثل ویرایش دستوارت دیتا ویو هست فقط با این تفاوت که همه دستورات باید در یک خط باشند و نباید اینتر بخورند. همچنین باید در ساختار `<!-- QueryToSerialize: <query> -->` قرار بگیرند. به عنوان نمونه دستوری که برای استخراج اطلاعات کتاب استفاده می کردم به این شکل می شود:
```md {1}
<!-- QueryToSerialize: table without id ("![](" + cover_self + ")"), file.name, "`" + pages + " صفحه`", rating FROM #Books WHERE date >= date("2024-03-20") AND date <= date("2025-03-20") SORT date DESC -->
<!-- SerializedQuery: table without id ("![](" + cover_self + ")"), file.name, "`" + pages + " صفحه`", rating FROM #Books WHERE date >= date("2024-03-20") AND date <= date("2025-03-20") SORT date DESC -->

| ("![](" + cover_self + ")") | file.name | "`" + pages + " صفحه`" | rating |
| --------------------------- | --------- | ---------------------- | ------ |
<!-- SerializedQuery END -->
```

جزئیات بیشتر رو می تونید توی [داکیومنت پلاگین دیتاویو سریالیزر](https://developassion.gitbook.io/obsidian-dataview-serializer) مشاهده کنید تا بیشتر با فضاش آشنا بشید.
جدول ها به صورت خودکار تولید میشن و هر موقع اطلاعات آپدیت بشن جداول هم آپدیت میشن. اگر نشد Ctrl + P را بزنید تا بخش command باز شود. دستور Scan and serialize all Dataview queries را جستجو و اجرا کنید.

(البته من از یک روش عجیب تر استفاده میکنم. وقتی فایل رو ویرایش کنید یعنی متنی رو اضافه یا کم کنید دیتایو سریالیزر فعال میشه و اسکن میکنه. من هم بعضی وقتا همین کار رو انجام میدم. مثلا یک 0 تایپ میکنم تا پلاگین فعال بشه و جدول رو آپدیت کنه. بعدش 0 رو پاک میکنم. یه کم عجیبه ولی خب کار میکنه حتی از خود اون دستور scan بهتر کار میکنه! چون بعضی وقتا اون دستور رو میزنم هیچ اتفاقی نمیفته)

<br> 

### چه طور والت اصلی را به والت کوارتز متصل کنیم؟
اگه والتی که برای انتشار استفاده می‌کنید همون والتی باشه که محتواها رو ثبت و عادت‌ها رو ردیابی می‌کنید، هیچ مشکلی ندارید و خیلی راحت می‌تونید با دیتاویو سریالیزر اطلاعات رو استخراج کنید. اما اگه مثل من از دو والت جداگانه استفاده می‌کنید، با یه چالش روبرو می‌شید. اینکه اطلاعات توی یه والت هست و سایت توی یه والت دیگه.

برای حل این مشکل می‌تونید از Symbolic Link یا Junction استفاده کنید. با این روش در واقع یه کپی از فایل یا فولدرتون می‌سازید که به نسخه اصلیش لینک شده. این لینک دو طرفه‌ست و هر نسخه‌ای که تغییر کنه، بلافاصله نسخه دیگه هم تغییر می‌کنه.

<br> 

**لینک دایرکتوری** <br> 
اگه می‌خواید یه فولدر رو لینک کنید، می‌تونید از Junction استفاده کنید. فرض کنید مسیر والت‌های شما اینجوریه:

📂 والت نوت: `C:\notes`

📂 والت کوارتز: `C:\quartz`

حالا می‌خواید یه فولدر به اسم "share" بسازید که تو هر دو والت دیده بشه. پس یک پوشه با همین اسم توی والت کوارتز بسازید. بعد از دستور زیر توی cmd استفاده کنید:

```bat
mklink /J "C:\notes\share" "C:\quartz\share"
```

در این روش حتما باید دقت کنید که مثل دستور بالا فایل اصلی توی **والت کوارتز** باشه و نسخه لینک شده توی **والت نوت**. اگه لینک رو برعکس درست کنید - یعنی فایل اصلی توی والت نوت باشه و فایل لینک شده توی والت سایت - کوارتز ارور میده و نمی‌تونه فایل رو منتشر کنه.

علاوه بر این، می‌تونید از Symbolic Link هم برای لینک کردن دایرکتوری استفاده کنید. (البته این روش فقط از کوارتز ۴.۳.۱ به بعد کار می‌کنه و تو نسخه‌های قبلی ارور می‌ده [+](https://github.com/jackyzha0/quartz/pull/1277)) به این شکل:

```bat
mklink /D "C:\notes\share" "C:\quartz\share"
```

<br>

**لینک فایل** <br>
اگه می‌خواید فقط یه فایل رو لینک کنید، می‌تونید از Symbolic Link استفاده کنید. برای این کار از دستور زیر استفاده کنید:

```bat
mklink "C:\notes\file.md" "C:\quartz\file.md"
```

<br> 

در مورد این روش توی یادداشت [[symlink|لینک نمادین]] بیشتر توضیح دادم.

<br> 

> [!red] 🚨توجه
>لازم نیست پلاگین دیتاویو سریالیزر رو توی والت کوارتز نصب کنید. چون با این کار سریالیزر دوباره اطلاعات رو اسکن می‌کنه و از اونجایی که داده‌ای توی والت کوارتز وجود نداره، داده‌های شما رو نشون نمی‌ده. البته می‌تونید از بخش تنظیمات پلاگین مشخص کنید که فایل‌های موجود توی یه فولدر خاص اسکن نشه.
> ![[1748365722954.webp]]
> 
 
 %%